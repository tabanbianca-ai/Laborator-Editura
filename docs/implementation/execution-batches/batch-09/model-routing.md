# Model Routing

Model routing selects the best approved model for the task using quality, risk, rights, language support, modality, data policy, latency, availability, and budget.

The cheapest model is not the primary criterion.

Routing must reject disabled, unavailable, incompatible, or over-budget models.

Fallback routing may run only through the provider fallback policy and must preserve all data protection and rights constraints.

