# Cost Control

Cost control records provider, model, agent, execution, input units, output units, total cost, currency, budget scope, budget id, and created time.

Hard budget enforcement is server-side.

Cost-aware routing considers quality, risk, rights, latency, availability, and budget before cost.

When a budget is exhausted, restricted AI actions are blocked; data is never deleted.

