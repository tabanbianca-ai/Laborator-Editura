# Regression Suite

Regression runs compare changed AI assets against approved datasets and historical baselines.

Prompt, model, provider, RAG, tool, and policy changes require regression evidence before production approval when they affect existing workflows.

Shadow evaluation may compare production and candidate models without changing user-facing behavior.

Failed regression results block promotion until reviewed.

