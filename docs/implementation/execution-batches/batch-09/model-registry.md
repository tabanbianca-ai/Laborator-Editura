# Model Registry

The Model Registry records approved AI models, provider model ids, versions, model type, modalities, supported languages, context limits, capabilities, risk profile, cost profile, latency profile, data processing policy, and status.

Only active registered models may be used in production.

Model changes require a change record, regression checks, and audit before production use.

The model version is part of execution records, semantic cache keys, and evaluation history.

