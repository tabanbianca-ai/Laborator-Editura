# Layout Engine

The layout engine uses `LayoutProfile` and existing Layout Publishing records.

Responsibilities:

- page size
- margins
- bleed
- crop marks
- widow/orphan controls
- structural style mappings
- print provider profile references

The layout engine does not own manuscript text. It reads approved semantic
master content and produces derived layout outputs.

