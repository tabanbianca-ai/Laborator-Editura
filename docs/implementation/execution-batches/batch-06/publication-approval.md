# Publication Approval

Publication approval is a human review of one exact build and package.

Rules:

- Approval is tied to `buildId`.
- Approval is tied to `validationReportId`.
- Approval is tied to `packageId`.
- Human Final Authority is required.
- Publication approval is separate from public visibility.
- Public visibility and external distribution are handled by later batches.

