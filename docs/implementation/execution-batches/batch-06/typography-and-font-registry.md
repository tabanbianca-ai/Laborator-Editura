# Typography And Font Registry

`TypographyProfile` defines typography choices for a build.

`FontRegistryEntry` defines:

- family name
- license reference
- embedding policy
- allowed formats
- validation status

Font embedding is allowed only when the font is validated and the target output
format is permitted by the embedding policy.

