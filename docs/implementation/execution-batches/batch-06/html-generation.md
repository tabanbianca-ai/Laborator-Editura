# HTML Generation

HTML generation produces a derived reading artifact from the approved semantic
master document.

Rules:

- Preserve semantic structure.
- Use approved metadata.
- Use controlled style mapping.
- Preserve accessibility attributes when available.
- Never modify the master document.

