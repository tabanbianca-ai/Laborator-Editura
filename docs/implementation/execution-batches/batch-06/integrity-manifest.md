# Integrity Manifest

The Integrity Manifest records checksums for every asset included in the
publication package.

Each item stores:

- asset id
- URI
- SHA256 checksum
- optional size

The manifest allows deterministic package verification.

